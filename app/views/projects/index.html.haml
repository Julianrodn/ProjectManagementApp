%h1 Projects#index
%p Find me in app/views/projects/index.html.erb
%h1.text-2xl.font-bold Proyectos

-# if user_signed_in?
-# end

%ul
  - @projects.each do |project|
    %li
      = link_to project.name, project_path(project), class: "text-blue-500 hover:underline"
= link_to 'Nuevo Proyecto', new_project_path, class: "btn btn-primary mb-4"
