= link_to 'Atrás', project_story_path(@project, @story), class: "btn btn-secondary"

%h1= @ticket.name
%p= @ticket.description
%p= "Estado: #{@ticket.status}"
%p= @ticket.comments

= link_to 'Editar', edit_project_story_ticket_path(@project, @story, @ticket), class: "btn btn-secondary"
= button_to 'Eliminar', project_story_ticket_path(@project, @story, @ticket), method: :delete, class: "btn btn-danger", onclick: "return confirm('¿Estás seguro de que deseas eliminar este ticket?');"



